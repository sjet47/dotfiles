# 20-cli.txt — 日常 CLI —— 交互习惯全靠这组,迁移后第一时间要有
#
# 行尾注释是 pacman 自带的包描述,仅供辨认;逐步替换成"我为什么需要它"更有价值。
# [AUR] 标记的包必须用 paru/yay 安装,纯 pacman 装不了。

# ── shell / 多路复用 / 历史 ─────────────────────────────────────────────
zsh       # A very advanced and programmable command interpreter (shell) for
tmux      # Terminal multiplexer
zellij    # A terminal multiplexer
starship  # The cross-shell prompt for astronauts
atuin     # Magical shell history

# ── 文件浏览与列表 ───────────────────────────────────────────────────────
lsd   # Modern ls with a lot of pretty colors and awesome icons
yazi  # Blazing fast terminal file manager written in Rust, based on asy
fd    # Simple, fast and user-friendly alternative to find

# ── 搜索 / 查看 ───────────────────────────────────────────────────────
ripgrep   # A search tool that combines the usability of ag with the raw spe
ugrep     # ultra fast grep with interactive TUI, fuzzy search, boolean quer
ast-grep  # CLI tool for code structural search, lint and rewriting
bat       # Cat clone with syntax highlighting and git integration
fzf       # Command-line fuzzy finder
hexyl     # Colored command-line hex viewer

# ── JSON / YAML / Markdown ────────────────────────────────────────
jq       # 脚本解析 hyprctl JSON(截图/录屏选区)
yq       # Command-line YAML, XML, TOML processor - jq wrapper for YAML/XML
gron     # Makes JSON greppable
jnv-bin  # [AUR] Interactive JSON filter using jq
mdq      # [AUR] like jq but for Markdown: find specific elements in a md doc
glow     # Command-line markdown renderer

# ── git 工具链 ───────────────────────────────────────────────────────
lazygit       # Simple terminal UI for git commands
github-cli    # The GitHub CLI
glab          # Cli tool to help work seamlessly with GitLab from the command li
git-delta     # Syntax-highlighting pager for git and diff output
difftastic    # An experimental diff tool that compares files based on their syn
lucky-commit  # Customize your git commit hashes
tokei         # A blazingly fast CLOC (Count Lines Of Code) program

# ── 系统观测 ──────────────────────────────────────────────────────────
htop                 # Interactive process viewer
btop                 # A monitor of system resources, bpytop ported to C++
iotop                # View I/O usage of processes
smem                 # [AUR] Generate reports on memory usage.
duf                  # Disk Usage/Free Utility
dust                 # A more intuitive version of du in rust
lnav                 # A curses-based tool for viewing and analyzing log files
hl                   # A fast and powerful log viewer and processor
systemd-manager-tui  # [AUR] A TUI for managing systemd services
fastfetch            # 系统信息
cpufetch             # Simple yet fancy CPU architecture fetching tool
hwinfo               # Hardware detection tool from openSUSE
impala               # TUI for managing wifi
dmidecode  # 读 BIOS/主板/内存槽位硬件信息

# ── 文档 / 图形 / 排版 ──────────────────────────────────────────────────
graphviz        # Graph visualization software
graphicsmagick  # Image processing system
resvg           # SVG rendering library and CLI
typst           # A markup-based typesetting system for the sciences
hugo            # Fast and Flexible Static Site Generator in Go
presenterm      # A markdown terminal slideshow tool

# ── 传输 / 下载 ───────────────────────────────────────────────────────
rclone  # rsync for cloud storage
qrcp    # [AUR] Transfer files over WiFi from computer to mobile by scanning a Q
yt-dlp  # A youtube-dl fork with additional features and fixes

# ── 杂项 ────────────────────────────────────────────────────────────
moreutils        # A growing collection of the unix tools that nobody thought to wr
pv               # A terminal-based tool for monitoring the progress of data throug
bc               # An arbitrary precision calculator language
just             # A handy way to save and run project-specific commands
shfmt            # Format shell programs
tldr             # Command line client for tldr, a collection of simplified man pag
gum              # A tool for glamorous shell scripts
zoxide           # A smarter cd command for your terminal
tree-sitter-cli  # CLI tool for developing, testing, and using Tree-sitter parsers
inotify-tools  # inotifywait/inotifywatch,脚本里监听文件变更
trash-cli      # trash-put/list/restore/empty,命令行走回收站而不是真删
yamllint       # YAML lint(ruff 管 Python,shfmt 管 shell,这个补 YAML)
