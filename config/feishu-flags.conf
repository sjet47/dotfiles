# 飞书 Wayland 原生试验配置
# /usr/bin/feishu 会读取本文件并追加到启动参数
# 若 Wayland 下输入法/截图/托盘有问题,删除本文件即可回退到 XWayland

# 走 Wayland 原生(自动跟随显示器 1.5 scale,HiDPI 更清晰)
--ozone-platform=wayland

# fcitx5 中文输入:Wayland 下必须显式开启 IME 支持
--enable-wayland-ime
--wayland-text-input-version=3
