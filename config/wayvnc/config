# 只监听回环:外部一律走 ssh 隧道接入(ssh -L 5901:127.0.0.1:5900 <ssh-target>,
# 端口/跳板之类的写进 ~/.ssh/config),因此不再需要 VNC 自身的密码/TLS ——
# 隧道之外没有任何东西能连到这个端口。
#
# 本地那头用 5901 而不是 5900:两头同号的话,自己连自己时 ssh 会先占住 5900,
# 轮到 wayvnc 去 bind 就失败了。错开也顺带避开本地已经占着 5900 的情况。
address=127.0.0.1
port=5900
