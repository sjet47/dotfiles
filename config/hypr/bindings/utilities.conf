# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod ALT, L, exec, hyprlock
bind = $mainMod ALT, M, exec, wlogout -p layer-shell
bind = $mainMod, Q, killactive,
bind = $mainMod, RETURN, exec, uwsm-app -- $terminal
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, T, togglefloating,
bind = $mainMod, space, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, S, layoutmsg, togglesplit # dwindle
bind = SUPER, V, exec, cliphist list | vicinae dmenu | cliphist decode | wl-copy
# 区域截图：SUPER+SHIFT+S 拉框 → ksnip 编辑（在 ksnip 内 Ctrl+S 存盘 / Ctrl+Shift+C 复制）
# ksnip 1.10 没有 stdin pipe 模式，只能 -e 吃文件，所以先 grim 到临时文件
bind = SUPER SHIFT, S, exec, bash -c 'f=$(mktemp --suffix=.png) && grim -g "$(slurp)" "$f" && ksnip -e "$f"'
